#pragma once
// Copy to secrets.h and fill in. Empty WIFI_SSID disables Wi-Fi/MQTT (firmware still builds).

#define WIFI_SSID     ""
#define WIFI_PASSWORD ""

// Home Assistant / Mosquitto — ignored unless WIFI_SSID is set
#define MQTT_HOST     "homeassistant.local"  // or 192.168.x.x / Tailscale IP
#define MQTT_PORT     1883
#define MQTT_USER     ""
#define MQTT_PASSWORD ""
#define MQTT_BASE     "geek/sw3518"

// Optional: override HA discovery prefix (default homeassistant)
// #define MQTT_DISCOVERY_PREFIX "homeassistant"
